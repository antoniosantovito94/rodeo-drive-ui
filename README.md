# Rodeo Drive UI

Homepage responsive statica costruita dai mock-up in `design_web_app`.

## Avvio

Apri `index.html` nel browser.

La pagina usa:

- `index.html` per struttura e contenuti
- `styles.css` per layout responsive e stile Bauhaus
- `assets/` per le immagini prodotto locali
